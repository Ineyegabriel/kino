import { Colors } from '@/assets/Colors';
import { css } from 'styled-components';

export const CancelFactorySettingsOnUnorderedList = css`
  list-style-type: none;
  margin-block: 0;
  margin-inline: 0;
  padding-inline-start: 0;
`;

export const StyledPill = css`
  background-color: ${Colors.Information100};
  align-items: center;
  background-color: #f1eaff;
  border-radius: 30px;
  display: inline-flex;
  position: relative;
  padding: 4px 12px;
`;
